generator client {
  provider        = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
  schemas  = ["public", "platform", "auth", "patients", "scheduling", "emr", "finance", "communications", "inventory", "hr", "analytics", "integrations", "billing"]
}
